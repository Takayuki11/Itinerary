<nav class="navbar navbar-expand-sm navbar-dark bg-info">
    <% if current_user %>
        <%= link_to "Itinerary", places_path, class: "text-white", data: {"turbolinks" => false} %>
    <% else %>
        <%= link_to "Itinerary", root_path, class: "text-white" %>
    <% end %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
        <span class="navbar-toggler-icon"></span>
    </button>
      
    <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
        <ul class="navbar-nav">
            <% if logged_in? %>
                <li class="nav-item"><%= link_to "share your place", new_place_path, class: "nav-link" %></li>
                <li class="nav-item"><%= link_to "All users", users_path, class: "nav-link" %></li>
                <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown"><%= current_user.name %></a>
                <ul class="dropdown-menu dropdown-menu-right">
                    <li class="dropdown-item"><%= link_to "Profile", user_path(current_user) %></li>
                    <li class="dropdown-item"><%= link_to "Logout", logout_path, method: :delete %></li>
            <% else %>
                <li class="nav-item"><%= link_to "Sign up", signup_path, class: "nav-link" %></li>
                <li class="nav-item"><%= link_to "Login", login_path, class: "nav-link" %></li>
            <% end %>
        </ul>
    </div>
</nav>